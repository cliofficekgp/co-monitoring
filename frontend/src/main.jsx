import React from "react";
import ReactDOM from "react-dom/client";
import "./globals.css"; // Ensure this file exists
import WhatsAppMonitor from "./App";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <WhatsAppMonitor />
  </React.StrictMode>
);
